#include "main.h"
/**
* _isupper - checking for uppercase character
* @c: Variable text to be checked
* Return: if uppercase character return 1 else return 0
*/
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
